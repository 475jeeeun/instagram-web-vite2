name: Github Actions 실행시켜보기

on:
   push:
    branches:
     - main 
jobs:
   my-deploy-job:
      runs-on: ubuntu-latest

   

      steps:
         - name: Hello world 출력하기
           run: echo "Hello, World!"